<?xml version="1.0" encoding="utf-8"?>
<!--
	Copyright (C) 2011, Comodo Group, Inc. & DNS.com, LLC
	All Rights Reserved
-->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" android:layout_height="fill_parent"
    android:orientation="vertical" android:background="@color/red" android:weightSum="1" 
    android:id="@+id/domain_hosts_activity">

    <!-- First view is a text editor.  We want it to use all available
         horizontal space, and stretch to fill whatever vertical space
         is available to it.  Note the use of the "id" attribute, which
         allows us to find this object from the Java code. -->

    <!-- Next view is another linear layout manager, now horizontal.  We
         give it a custom background; see colors.xml for the definition
         of drawable/semi_black-->
    <ImageView android:id="@+id/dnsLogo" android:layout_width="fill_parent" 
    	android:layout_height="wrap_content" android:src="@drawable/dns_android_banner"
    	android:layout_alignParentTop="true"></ImageView>
    <TextView android:layout_height="wrap_content" android:textColor="@color/white" 
    	android:layout_width="fill_parent" android:background="@color/dark_grey" android:padding="4px" 
    	android:gravity="center_vertical|left" android:id="@+id/groupHeaderLabel" 
    	android:text="@string/domain_groups_list_label" android:layout_below="@id/dnsLogo"></TextView>
    <EditText android:layout_height="wrap_content" android:layout_width="fill_parent" 
    	android:id="@+id/filterInput" android:hint="Filter" android:layout_below="@id/groupHeaderLabel">
    </EditText>
    <TextView android:layout_height="wrap_content" android:id="@+id/helpText" 
    	android:text="@string/screen_bottom_help_text" android:layout_width="fill_parent" 
    	android:layout_gravity="center_horizontal" android:background="@color/dark_grey" 
    	android:textColor="@color/white" android:focusable="false" android:focusableInTouchMode="false"
    	android:layout_alignParentBottom="true"></TextView>
    <ListView android:cacheColorHint="@color/red" android:dividerHeight="2.0sp" 
    	android:layout_height="wrap_content" android:layout_width="fill_parent" android:id="@+id/groupListView" 
    	android:visibility="invisible" 
    	android:focusable="true" android:focusableInTouchMode="true" android:layout_below="@id/filterInput"
    	android:layout_above="@id/helpText">
        <requestFocus></requestFocus>
    </ListView>
    <ProgressBar android:layout_height="wrap_content" android:layout_width="wrap_content" 
    	android:id="@+id/groupListProgressBar" android:layout_gravity="center" android:focusable="false" 
    	android:focusableInTouchMode="false" android:layout_centerInParent="true"></ProgressBar>
    <TextView android:layout_height="wrap_content" android:id="@+id/helpText" 
    	android:text="@string/screen_bottom_help_text" android:layout_width="fill_parent" 
    	android:layout_gravity="center_horizontal" android:background="@color/dark_grey" 
    	android:textColor="@color/white" android:focusable="false" android:focusableInTouchMode="false"
    	android:layout_alignParentBottom="true"></TextView>
</RelativeLayout>


